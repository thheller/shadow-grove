(ns shadow.grove.css.defs)

;; THIS FILE MUST REMAIN EMPTY!!!
;; the build hook will generate the actual source for it
;; just using it as an empty placeholder so everything is declared and in the correct compilation order
